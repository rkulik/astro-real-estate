---
interface Props {
  icon?: string;
  class?: string;
}

const { icon, class: className } = Astro.props;
---

<div class:list={['flex items-center gap-2', className]}>
  {icon && <span>{icon}</span>}
  <span>
    <slot />
  </span>
</div>
