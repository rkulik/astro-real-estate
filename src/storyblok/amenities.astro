---
import { storyblokEditable } from '@storyblok/astro';

import Feature from '../components/feature.astro';
import type { AmenitiesStoryblok } from '../types/component-types-sb';

interface Props {
  blok: AmenitiesStoryblok;
}

const { blok } = Astro.props;
---

<section {...storyblokEditable(blok)}>
  <h2 class="mb-6 text-2xl font-bold">{blok.title}</h2>
  <div class="grid grid-cols-2 gap-y-4 sm:grid-cols-3 md:grid-cols-2 xl:grid-cols-3">
    {
      blok.features.map((blok) => {
        return (
          <Feature>
            <span slot="label">{blok.label}</span>
            <p slot="description">{blok.description}</p>
          </Feature>
        );
      })
    }
  </div>
</section>
